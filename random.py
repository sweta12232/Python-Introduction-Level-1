# The random module in Python helps us make things unpredictable in our code, like rolling dice or shuffling cards in a game. 
# It lets us generate random numbers or pick random items from a list, adding an element of surprise or randomness to your program.

import random
randomNum = random.randint(1, 100)
print("Random number:", randomNum)
